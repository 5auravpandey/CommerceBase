# Active Task List
- [x] 1. Audit and sanitize all JSPs for XSS vulnerabilities using `<c:out>`.
- [x] 2. Restyle `header.jsp`, `products.jsp`, `cart.jsp`, and `dashboard.jsp` using strict `design.md` constraints.
- [x] 3. Update `web.xml` to mask Tomcat errors and build a mature `error.jsp`.
- [x] 4. Clean all Java files of AI-like comments and add author headers.
- [x] 5. Update `memory.md` with the changelog.
